# Python dependencies for traffic-signal-control
# NOTE: PyTorch is installed separately in setup_server.sh (CPU build) to avoid
#       pulling a multi-GB CUDA wheel — PPO's MlpPolicy trains on CPU.
gymnasium>=0.29,<1.2
numpy>=1.24
stable-baselines3>=2.8.0
tensorboard>=2.14
# TraCI must be pip-installed: traffic_env.py imports traci before adding
# $SUMO_HOME/tools to sys.path.
traci>=1.20
sumolib>=1.20
